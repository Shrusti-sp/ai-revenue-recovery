# Notebook guide

Use this folder for exploratory copies of the reproducible application flow: `01_data_generation`, `02_eda`, and `03_modeling`. The production path is the tested modules under `src/`; notebooks should never become the source of truth for dashboard calculations.
