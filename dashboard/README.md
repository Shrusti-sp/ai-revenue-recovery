# Dashboard pages

The Streamlit pages are orchestrated in `app.py` to keep shared filtering and the generated analytics state consistent. The page set is Executive Overview, AI Risk Monitor, Revenue Recovery, Customer 360, AI Decision Center, AI Analytics Assistant, and Model Evaluation.
